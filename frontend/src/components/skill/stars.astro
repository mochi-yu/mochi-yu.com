---
interface Props {
  filled_count: number;
}

const { filled_count } = Astro.props;
const dummyArray = [0, 0, 0, 0, 0];
---

<div id='stars'>
  {
    dummyArray.map((_, index) =>
      index >= filled_count ? (
        <img src='/images/star_blank.svg' />
      ) : (
        <img src='/images/star_fill.svg' />
      ),
    )
  }
</div>
<style>
  img {
    height: 18px;
    width: auto;
    margin-left: -2px;
  }

  #stars {
    display: flex;
  }
</style>
